/** Fixed 7-digit prefix for all garment size IDs (starts with 4). */
export const SIZE_ID_PREFIX = '4580871';

export const SIZE_ID_LENGTH = 12;

export const SIZE_ID_SUFFIX_LENGTH = SIZE_ID_LENGTH - SIZE_ID_PREFIX.length;
