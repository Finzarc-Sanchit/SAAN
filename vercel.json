{
  "version": 2,
  "installCommand": "cd server && npm install --include=dev",
  "buildCommand": "cd server && npm run build",
  "functions": {
    "server/api/index.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/server/api" }]
}
