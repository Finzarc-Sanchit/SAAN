import { AccountProfileDetailsPage } from '@/components/account/AccountProfileDetailsPage';

export const metadata = {
  title: 'My Account — SAAN',
  description: 'Manage your SAAN profile, addresses, and order history.',
};

export default function AccountPage() {
  return <AccountProfileDetailsPage />;
}
